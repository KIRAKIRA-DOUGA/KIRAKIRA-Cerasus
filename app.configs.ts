/* export default defineAppConfig({
	theme: {
		primaryColor: "#ababab",
	},
	showCssDoodle: false,
}); */
// 不知道咋用，反正原版的这个就是用不了。

export const defaults = {
	// 使用说明：
	// 逐个罗列各用户配置设置的键名及其默认值，如为高级类型，再默认值之后用 as 声明。
	showCssDoodle: false,
};
